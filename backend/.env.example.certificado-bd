# ============================================
# VARIABLES DE ENTORNO - CERTIFICADO PFX EN BD
# ============================================

# ENCRIPTACIÓN (NUEVO - REQUERIDO)
# Generar con: openssl rand -hex 32 (Linux/Mac)
#           o node backend/scripts/generate-encryption-key.js
#           o PowerShell: [System.Convert]::ToHexString([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
ENCRYPTION_KEY=<REEMPLAZA_CON_TU_CLAVE_DE_32_BYTES>

# CERTIFICADO MAESTRO
# true = usar BD (recomendado)
# false = usar archivo físico (legacy)
USAR_BD_PARA_CERTIFICADO=true

# ============================================
# CONFIGURACIÓN AFIP (EXISTENTE)
# ============================================
AFIP_CUIT=tu_cuit_aqui
AFIP_FABRICANTE=SE
AFIP_WSAA_URL=https://wsaa.afip.gov.ar/ws/services/LoginCms
AFIP_WSCERT_WSDL=https://servicios1.afip.gov.ar/wscert/service
AFIP_CERT_PATH=backend/certs/certificado.pfx
AFIP_KEY_PASSWORD=tu_contraseña_pfx
AFIP_ROOT_PATH=backend/certs/Root_RTI.txt

# ============================================
# CONFIGURACIÓN BASE DE DATOS
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=s3rs4
DB_PASSWORD=tu_password
DB_NAME=db_sersa
DB_SSL=false

# ============================================
# CONFIGURACIÓN APLICACIÓN
# ============================================
NODE_ENV=development
PORT=3000

# JWT
JWT_SECRET=tu_secret_jwt_aqui
JWT_EXPIRATION=24h

# ============================================
# THROTTLING (RATE LIMITING)
# ============================================
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# ============================================
# OTROS
# ============================================
# CORS_ORIGIN=http://localhost:3001
LOG_LEVEL=debug
